import 'package:flutter/material.dart';

class TaxBracketsView extends StatelessWidget {
  const TaxBracketsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tax Brackets View (Under Construction)'),
    );
  }
}
