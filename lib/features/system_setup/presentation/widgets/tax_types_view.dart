import 'package:flutter/material.dart';

class TaxTypesView extends StatelessWidget {
  const TaxTypesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tax Types View (Under Construction)'),
    );
  }
}
