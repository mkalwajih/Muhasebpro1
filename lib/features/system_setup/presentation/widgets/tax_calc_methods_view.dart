import 'package:flutter/material.dart';

class TaxCalcMethodsView extends StatelessWidget {
  const TaxCalcMethodsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tax Calculation Methods View (Under Construction)'),
    );
  }
}
